#include "main.h"

/**
 * mul - multiplies two integers
 *
 * @a: first input to multiply
 * @b: second input to multiply
 *
 * Return: return multiplication results
*/

int mul(int a, int b)
{
	return (a * b);
}
